import React  from 'react'

export default function StockList (props) {
    
    return (
        <div>
            { props.showStockArray ? 
            null:
            null
            }
                
        </div>
    )
    
}
